[options]
HoldPkg     = pacman glibc
# x86-64-v4: AVX-512 + everything below. Targets Intel Rocket Lake+
# (11th gen) on consumer side, AMD Zen 4+ (Ryzen 7000+ / EPYC Genoa+).
# Falls back to v3 and generic for anything not present in znver4 repos.
Architecture = x86_64 x86_64_v3 x86_64_v4
ParallelDownloads = 10
SigLevel    = Required DatabaseOptional
LocalFileSigLevel = Optional

# --- CachyOS znver4 (AVX-512 optimized) — preferred for v4 builds ---

[cachyos-znver4]
Include = /etc/pacman.d/cachyos-v4-mirrorlist
SigLevel = Never

[cachyos-core-znver4]
Include = /etc/pacman.d/cachyos-v4-mirrorlist
SigLevel = Never

[cachyos-extra-znver4]
Include = /etc/pacman.d/cachyos-v4-mirrorlist
SigLevel = Never

# --- CachyOS v3 fallback for packages not in znver4 ---

[cachyos-v3]
Include = /etc/pacman.d/cachyos-v3-mirrorlist
SigLevel = Never

[cachyos]
Include = /etc/pacman.d/cachyos-mirrorlist
SigLevel = Never

# --- Standard Arch Linux Repositories ---

[core]
Include = /etc/pacman.d/mirrorlist

[extra]
Include = /etc/pacman.d/mirrorlist

[multilib]
Include = /etc/pacman.d/mirrorlist
