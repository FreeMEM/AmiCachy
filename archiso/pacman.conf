[options]
HoldPkg     = pacman glibc
Architecture = auto
ParallelDownloads = 10
SigLevel    = Never
LocalFileSigLevel = Optional

# --- CachyOS Repositories (x86-64-v3 optimized) ---

[cachyos-v3]
Include = /etc/pacman.d/cachyos-v3-mirrorlist
SigLevel = Never

[cachyos-core-v3]
Include = /etc/pacman.d/cachyos-v3-mirrorlist
SigLevel = Never

[cachyos-extra-v3]
Include = /etc/pacman.d/cachyos-v3-mirrorlist
SigLevel = Never

[cachyos]
Include = /etc/pacman.d/cachyos-mirrorlist
SigLevel = Never

# --- Standard Arch Linux Repositories ---

[core]
Include = /etc/pacman.d/mirrorlist
SigLevel = Never

[extra]
Include = /etc/pacman.d/mirrorlist
SigLevel = Never

[multilib]
Include = /etc/pacman.d/mirrorlist
SigLevel = Never
