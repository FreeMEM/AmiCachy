# SPDX-License-Identifier: CC0-1.0
#
# Enable NetworkManager on the installed system (configure_system did
# `systemctl enable NetworkManager`). amicachy-base already ships the
# performance/plymouth .wants symlinks, so they are not repeated here.
---
units:
    - name: "NetworkManager"
      action: "enable"
      mandatory: false
